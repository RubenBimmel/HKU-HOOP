#include "CalendarWeek.h"

//Gets called when new content needs to be displayed on the timetable
void CalendarWeek::display(SchoolClass &schoolClass) {
	for each (CalendarDate day in days)
	{
		//Get all lessons for schoolClass

		//Do some display stuff
	}
}